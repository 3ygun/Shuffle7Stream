function showCMatchMelee2(data) {
	// Update the text
	$matchTitle.html("" + data.title.toUpperCase());
    $leftName.html("" + data.leftName.toUpperCase());
    $rightName.html("" + data.rightName.toUpperCase());
	
	// Space text
}